# Test Repository

This repository is currently a minimal starter project.

## Repository housekeeping

This cleanup adds:

- A baseline `.gitignore` to avoid committing local or generated files.
- A standard `.editorconfig` for consistent formatting defaults.
- A clearer README structure for future contributors.