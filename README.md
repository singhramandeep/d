# Test Repository

Minimal repository used for basic Git and repository hygiene exercises.

## Repository hygiene

This repository includes a few baseline housekeeping files:

- `.gitignore` to avoid committing local/editor junk
- `.editorconfig` to keep formatting consistent
- `.gitattributes` to normalize text files across platforms